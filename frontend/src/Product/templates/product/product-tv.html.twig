{% extends '@layout/default.html.twig' %}

{% block title %}Smartphones{% endblock %}
{% block page_title %}{% endblock %}

{% block body_class %}product-smartphone{% endblock %}

{% block content %}
    <div class="container">
        <h1>Tv</h1>
        <div class="row">
            {% set i = 0 %}
            {% if tv is not empty %}
                {% for prodTV in tv %}
                    <div class="col-md-4 d-flex">
                        <div class="card mb-4 w-100">
                            <img class="rounded mx-auto d-block " src="/images/app/product/{{ prodTV.img }}.png"
                                 alt="Product Image" height="200px" width="200px">
                            <div class="card-body d-flex flex-column">
                                <a href="{{ path('product', {'action' : 'detail' , 'uuid' : prodTV.uuid.toString() } ) }}">
                                    <h3>{{ prodTV.title|e }}</h3>
                                </a>
                                <p>Description:</p>
                                <p class="card-text">{{ prodTV.description|e }}</p>
                                <p class="card-text" style="font-weight: bold; color: black;font-size: 1.2vw">
                                    Price: {{ prodTV.price|e }} $</p>
                                {% if stocks[i] != 0 %}
                                    <button data-id="{{ prodTV.uuid.toString() }}" type="button"
                                            class="btn btn-success mt-auto ml-5 mr-5 addToCart">
                                        <i class="fas fa-shopping-cart"></i> Add to cart
                                    </button>
                                {% else %}
                                    <button type="button" class="btn btn-danger mt-auto ml-5 mr-5" disabled>
                                        <i class="fas fa-shopping-cart"></i> Out of Stock
                                    </button>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                    {% set i = i + 1 %}
                {% endfor %}
            {% endif %}
        </div>
    </div>
{% endblock %}